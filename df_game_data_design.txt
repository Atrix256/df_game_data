Define schema:
 * struct containing:
  * pod types (float, int32, string, bool? more later?)
  * array of pod types  (index to item)
  * dictionary of pod types (string to item? or should it be more generic than that? pod to item? array is just a contiguous int to item)
 * root is... an object? could be a dictionary, but maybe should be user defined what the root is.
  * or maybe dictionary of string so we can show the records on the left and the data on the right?
  * or that is a special case?
 ! no assets. different project

Source data format:
 * ini file format? to be easy to merge?
 * a data file per record? what if it isn't a dictionary at the root?

Destination data format:
 * for baking. maybe user specified? could have an array of baking targets. each array entry gives a format and a destination

Editor
 * maybe imgui for now. vulkan / windows?
 * menu item to bake and have output of baking in window (separate exe instead of a library call? and show stdout in a log window?)

Baker
 * work from command line. should it bake a single target or all targets? maybe all targets for now, could be changed later if needed.
 * binary for now. could imagine option for compressed file later.
 * optionally create C header files for data to work with them natively instead of querying interface for data?
 * what about schema changes on hot reload

Runtime Library
 * load a packed data file.
 * give a callback to call on hot reload.
 * watch file via polling, so a tick() function is called each frame.
 * interface to query for data.

Other
 * maybe have angelscript for writing code for how to convert from one data type to another.
  * like for schema upgrades?
 * also maybe being able to have a separate definition for packed struct, instead of edited struct? idk.


============= ACTUALLY IMPLEMENTING =============

Dictionary is probably fine. one file per source data record. json?
Probably need a way to resave all source data files, like when you change the schema and have a field that is renamed or something.

============= TODO NEXT =============
* wxWidgets won't build release, it wants release dlls. i don't think i ever actually want / need the debug dlls of wxWidgets.
 * get rid of the dlls not in the main folder? or .git ignore them and make them be copied on build? idk
 * delete the "ud" dlls?
* make wxWidgets have a left panel listing db entries, and a right panel listing a property sheet.
* get rid of imgui
 * also don't link to dx anymore. maybe make a new project for wxWidgets just in case.
* load the yaml data / databases. show the list of records on the left panel. show the selected record on the right panel.
 * have a drop down for database to show.
* ryml is under editor project external folder. it should be under the loader. or maybe loader project shouldn't exist anymore idk.

============= TODO =============
* make an option to show the imgui demo window. show_demo_window
* put menu and main ui drawing into another file, away from the imgui boilerplate
? should i support undo / redo? read up on best practices for that?
* file watch change callback should give a void* user data back with the callback

yaml

* have a project yaml project file point to db yaml files which each describe a single root type (single database)
 * each db should be it's own folder.  the contents of the folder are kept, to find changes for hot reloading.

* document how to make a project and db
* document how to link to it for loading the data? (binary packing has headers that are standalone usable without the library)

icon for editor? anything will do for now

make editor.cpp control the window title, and have it say the file name opened, and have * when dirty

need native file dialogs. nfd again? or something else?